---
import Layout from '../layouts/SiteLayout.astro';
import { getCollection } from 'astro:content';

const essays = await getCollection('essays');
const sortedEssays = essays.sort((a, b) => {
  const aDate = a.data.published ? new Date(a.data.published).getTime() : 0;
  const bDate = b.data.published ? new Date(b.data.published).getTime() : 0;
  return bDate - aDate;
});
---
<Layout title="Research & Publications">
  <section class="space-y-10 max-w-4xl mx-auto">
    <p class="text-sm uppercase tracking-[0.45em] text-indigo-300/80">Research</p>
    <h1 class="text-4xl font-light uppercase tracking-[0.35em] text-white sm:text-5xl md:text-6xl">
      Epistemic Teaming in Practice
    </h1>
    <p class="text-lg text-slate-300 leading-relaxed">
      Cynchorus publishes exploratory essays and codex fragments at the intersection of AI architecture, ethics, and culture.
      Each piece serves as both documentation and experiment — testing how narrative can act as a feedback loop in technical systems.
    </p>
    <div class="grid gap-8 md:grid-cols-2">
      <article class="rounded-2xl border border-white/10 bg-white/5 p-8 backdrop-blur">
        <h2 class="text-lg font-semibold uppercase tracking-[0.25em] text-white">Essays</h2>
        <ul class="mt-6 space-y-3 text-sm text-slate-300">
          <li><a href="/research/epistemic-teaming" class="hover:text-white">Epistemic Teaming: How a Machine Learns to Argue Fairly</a></li>
          <li><a href="/research/choral-intelligence" class="hover:text-white">Choral Intelligence: Systems That Learn to Listen</a></li>
        </ul>
      </article>
      <article class="rounded-2xl border border-indigo-400/30 bg-indigo-400/10 p-8 backdrop-blur">
        <h2 class="text-lg font-semibold uppercase tracking-[0.25em] text-indigo-100">Codex Fragments</h2>
        <p class="mt-4 text-sm text-indigo-100/90 leading-relaxed">
          Field notes, dialogues, and visual experiments documenting the emergence of harmonic machine thinking.
        </p>
        <p class="mt-6 text-xs uppercase tracking-[0.3em] text-indigo-200/70">// codex fragment index coming soon</p>
      </article>
    </div>
  </section>
</Layout>

