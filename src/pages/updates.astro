---
import Layout from '../layouts/SiteLayout.astro';
import { getCollection } from 'astro:content';

const updates = await getCollection('updates');
const sortedUpdates = updates.sort((a, b) => {
  const aDate = a.data.published ? new Date(a.data.published).getTime() : 0;
  const bDate = b.data.published ? new Date(b.data.published).getTime() : 0;
  return bDate - aDate;
});
---
<Layout title="Cynchorus Updates">
  <section class="space-y-10 max-w-3xl mx-auto">
    <p class="text-sm uppercase tracking-[0.45em] text-indigo-300/80">Updates</p>
    <h1 class="text-4xl font-light uppercase tracking-[0.35em] text-white sm:text-5xl md:text-6xl">
      Field Notes & Developments
    </h1>
    <p class="text-lg text-slate-300 leading-relaxed">
      Progress reports from the lab — software prototypes, philosophical drafts, and system design notes captured as part of
      the ongoing R&D cycle.
    </p>
    <article class="rounded-2xl border border-white/10 bg-white/5 p-8 backdrop-blur">
      <h2 class="text-lg font-semibold uppercase tracking-[0.25em] text-white">Latest Log</h2>
      <p class="mt-4 text-sm text-slate-300 leading-relaxed">
        The Astro front-end and Tailwind architecture are now live. Phase 1 focuses on turning theoretical frameworks into
        interactive documentation. Next: integrating live Bias-Map and Divergence Index visualisations.
      </p>
      <p class="mt-6 text-xs uppercase tracking-[0.3em] text-indigo-300/80">// update 2025-10-27 — initial deploy complete</p>
    </article>
  </section>
</Layout>

